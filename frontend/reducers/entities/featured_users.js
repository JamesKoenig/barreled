import {
  RECEIVE_FEATURED_USERS,
} from "../../actions/featured_users_actions";

export default (oldState = {}) => {
  Object.freeze(oldState);
  //switch statement would go here
  return oldState;
}
